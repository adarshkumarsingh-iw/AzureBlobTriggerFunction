export enum FILE_EXTENTION_TYPE {
  PARQUET = "parquet",
  XLSX = "xlsx",
}

export enum DATA_TYPES {
  SKU = "SKU",
  SKU_REVIEWS = "SKU_REVIEWS",
}
export const DATA_TYPES_LIST = Object.keys(DATA_TYPES);
